export type ZIndices = {
  ribbon: number;
  dropdown: number;
  modal: number;
};

export type MediaQueries = {
  xs: string;
  sm: string;
  md: string;
  lg: string;
  xl: string;
  xxl: string;
};